pipeline {
    agent any
    environment {
        PATH = "${tool('sbt')}:$PATH"
    }
    stages {
        stage('Build') {
            steps {
                ansiColor('xterm') {
                    sh 'sbt compile'
                }

            }
        }
    }
}
