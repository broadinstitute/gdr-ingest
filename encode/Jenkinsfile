pipeline {
    agent any
    environment {
        PATH = "${tool('sbt')}${File.pathSeparator}$PATH"
    }
    stages {
        stage('Build') {
            steps {
                sh 'sbt compile'
            }
        }
    }
}
